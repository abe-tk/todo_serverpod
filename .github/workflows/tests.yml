name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      VERSION: '3.3.1'
      SERVERPOD_PASSWORD_database: 'BDMgMmfCROP6RISjDh2turA-kz8NtJo4'
      SERVERPOD_PASSWORD_redis: 'psj6ZtpnS6xw8I9UJsHfALqjQ30X0SZs'

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.8'

      - name: Start containers
        working-directory: todo_serverpod_server
        run: docker compose up --build -d

      - name: Install dependencies
        working-directory: todo_serverpod_server
        run: dart pub get

      - name: Install Serverpod CLI
        working-directory: todo_serverpod_server
        run: dart pub global activate serverpod_cli $VERSION

      - name: Run Serverpod generator
        working-directory: todo_serverpod_server
        run: serverpod generate

      - name: Run tests
        working-directory: todo_serverpod_server
        run: dart test

      - name: Stop containers
        if: always()
        working-directory: todo_serverpod_server
        run: docker compose down -v
